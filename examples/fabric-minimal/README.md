# Fabric Minimal Example

This directory documents the minimum metadata shape Keystone expects in a Fabric project:

- `settings.gradle.kts` or `build.gradle.kts`
- Java 21 build configuration
- `src/main/resources/fabric.mod.json` with `schemaVersion`, `id`, `version`, and `depends`
